Disables Jekyll on GitHub Pages.

The site is plain HTML, CSS, JavaScript and JSON, served as committed. Without
this file Pages runs the tree through Jekyll first: slower to deploy, and it
drops any file or directory whose name starts with an underscore.
